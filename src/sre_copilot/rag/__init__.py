# Marks "mcp_tools.rag" as a Python package. It holds the hybrid
# (vector + keyword) retrieval-augmented-generation pipeline for searching
# past incidents: chunking, embedding, storage, ingestion, and retrieval.
