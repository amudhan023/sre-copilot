# Marks "agent" as a Python package. It holds the LangGraph-based SRE
# investigation agent: the shared state shape, the graph nodes, the LLM call
# layer (with Gemini/Groq fallback), and the MCP-to-Gemini tool conversion.
