# Marks "tests.evaluation" as a Python package so its test modules get a
# unique dotted name and don't collide with same-named modules in tests/
# (e.g. tests/test_metrics.py vs tests/evaluation/test_metrics.py).
